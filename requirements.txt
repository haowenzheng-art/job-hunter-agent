# ===========================================================================
# Job Hunter v2.1 — 依赖入口
# ===========================================================================
# 这个文件不再是真理之源，仅作向后兼容入口。
# - 编辑依赖：改 requirements.in（loose pin），跑 `pip-compile` 重新生成 lock
# - 安装：pip install -r requirements.lock   （CI 也走这条）
# - 紧急添加单个包：pip install <pkg>，然后把它加到 requirements.in
# ---------------------------------------------------------------------------
-r requirements.lock
